# Survey Link Generator - User Flow Diagrams

## Before Refactoring

```
User Action                    System Response
───────────                    ───────────────

1. Click "Survey Link          → Shows empty page with 
   Generator" tab                survey selector

2. Search for survey           → Displays survey options

3. Select survey               → Loads invitation stats
                                 Shows "Generate Links" button

4. Click "Generate Links"      → Opens modal with 
   button                        link count field

5. Enter link count            → Validates input

6. Click "Generate"            → Creates links
                                 Shows success modal
```

**Total Steps: 6 interactions**

---

## After Refactoring

```
User Action                    System Response
───────────                    ───────────────

1. Click "Survey Link          → Modal opens automatically! 🎉
   Generator" tab                Shows:
                                 - Survey selector
                                 - Link count field (default: 10)
                                 - Generate button (disabled)

2. Search & select survey      → Generate button becomes enabled
                                 Error message clears (if shown)

3. Adjust link count           → Validates 1-200 range
   (optional)                    

4. Click "Generate"            → Creates links
                                 Shows success modal
```

**Total Steps: 4 interactions (33% reduction!)**

---

## Component States Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     surveyInvitations Component              │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
                ┌─────────────┴──────────────┐
                │                            │
          Has recordId?              No recordId?
         (Record Page)                (Tab Page)
                │                            │
                │                            │
                ▼                            ▼
    ┌───────────────────────┐   ┌──────────────────────────┐
    │ Normal Component View │   │ Auto-Open Modal          │
    │ - Stats displayed     │   │ - Modal opens on load    │
    │ - Generate button     │   │ - Survey selector shown  │
    │ - Data table          │   │ - Link count shown       │
    └───────────────────────┘   └──────────────────────────┘
                │                            │
                │                            │
    User clicks "Generate"      User selects survey & count
                │                            │
                └────────────┬───────────────┘
                             │
                             ▼
                ┌─────────────────────────┐
                │  Generate Links Modal   │
                │  - Creates invitations  │
                │  - Shows success modal  │
                └─────────────────────────┘
```

---

## Modal State Machine

```
┌──────────────┐
│  Modal Opens │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────┐
│  Initial State                          │
│  - showGenerateModal = true             │
│  - showSurveyRequiredMessage = false    │
│  - linkCount = 10                       │
│  - isGenerateDisabled = true (no survey)│
└──────┬──────────────────────────────────┘
       │
       ├──────────────────────────────────┐
       │                                  │
       ▼                                  ▼
┌─────────────────┐            ┌──────────────────┐
│ Survey Selected │            │ User Clicks      │
│                 │            │ Generate         │
│ Effect:         │            │ (No Survey)      │
│ - Enable button │            │                  │
│ - Load data     │            │ Effect:          │
│ - Clear errors  │            │ - Show warning   │
└────────┬────────┘            │ - Show toast     │
         │                     │ - Keep modal open│
         │                     └──────────────────┘
         ▼                              │
┌─────────────────┐                    │
│ User Adjusts    │                    │
│ Link Count      │                    │
│                 │                    │
│ Validation:     │◄───────────────────┘
│ - Min: 1        │  User selects survey
│ - Max: 200      │  to fix error
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────┐
│ User Clicks Generate            │
│ (Survey Selected)               │
│                                 │
│ Actions:                        │
│ 1. Validate survey & count      │
│ 2. Call createBulkInvitations() │
│ 3. Close generate modal         │
│ 4. Open links modal             │
│ 5. Refresh invitations table    │
└─────────────────────────────────┘
```

---

## Validation Flow

```
handleGenerateLinks()
       │
       ▼
┌─────────────────┐
│ Check Survey ID │───── No ────► Show error message
└────────┬────────┘              Show warning icon
         │                       Keep modal open
         │ Yes
         ▼
┌─────────────────┐
│ Check Link Count│───── Invalid ──► Show error toast
│ (1-200 range)   │                  Keep modal open
└────────┬────────┘
         │ Valid
         ▼
┌─────────────────────┐
│ Generate Links      │
│ - Set isGenerating  │
│ - Call Apex         │
│ - Handle response   │
└─────────────────────┘
```

---

## Button State Logic

```javascript
// isGenerateDisabled computed property

┌─────────────────────────────────────┐
│ Button Disabled When:               │
│                                     │
│  isGenerating == true               │
│       OR                            │
│  !effectiveSurveyId                 │
│                                     │
│ Button Enabled When:                │
│                                     │
│  isGenerating == false              │
│       AND                           │
│  effectiveSurveyId exists           │
└─────────────────────────────────────┘
```

---

## Error Handling Strategy

```
┌──────────────────────────────────────────────────────────┐
│ Layer 1: UI Prevention                                   │
│ - Button disabled until survey selected                  │
│ - Cannot click Generate without survey                   │
└──────────────────┬───────────────────────────────────────┘
                   │ Bypass unlikely, but handled
                   ▼
┌──────────────────────────────────────────────────────────┐
│ Layer 2: Visual Feedback                                 │
│ - Warning message appears below survey selector          │
│ - Warning icon draws attention                           │
│ - Error color (red) indicates problem                    │
└──────────────────┬───────────────────────────────────────┘
                   │ User might miss visual cue
                   ▼
┌──────────────────────────────────────────────────────────┐
│ Layer 3: Toast Notification                              │
│ - Error toast appears at top of screen                   │
│ - "Please select a survey first" message                 │
│ - Cannot be missed                                       │
└──────────────────┬───────────────────────────────────────┘
                   │ All validation complete
                   ▼
┌──────────────────────────────────────────────────────────┐
│ Layer 4: Server Validation                               │
│ - Apex method validates survey ID                        │
│ - Returns error if invalid                               │
│ - Final safety net                                       │
└──────────────────────────────────────────────────────────┘
```

---

## Backwards Compatibility

```
┌────────────────────────────────────────┐
│ Component Load (connectedCallback)     │
└───────────────┬────────────────────────┘
                │
                ▼
         ┌──────────────┐
         │ Has recordId?│
         └──────┬───────┘
                │
      ┌─────────┴─────────┐
      │                   │
     Yes                 No
      │                   │
      ▼                   ▼
┌──────────────┐   ┌──────────────────┐
│ RECORD PAGE  │   │ STANDALONE TAB   │
│ (Legacy)     │   │ (New Behavior)   │
│              │   │                  │
│ - Load data  │   │ - Auto-open modal│
│ - Show stats │   │ - Show selector  │
│ - NO modal   │   │ - Guide user     │
│              │   │                  │
│ User clicks  │   │ Direct workflow  │
│ "Generate"   │   │ in modal         │
└──────────────┘   └──────────────────┘
      │                   │
      └─────────┬─────────┘
                │
                ▼
    ┌───────────────────────┐
    │ Modal Opens           │
    │ (Same for both)       │
    └───────────────────────┘
```

**Key Point**: The refactoring only affects the standalone tab experience. Record page behavior is completely unchanged, ensuring zero impact on existing workflows.

---

## Component Integration

```
┌─────────────────────────────────────────────────────────────┐
│                    Salesforce Org                           │
└─────────────────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┐
            │                 │                 │
            ▼                 ▼                 ▼
   ┌─────────────┐  ┌──────────────────┐  ┌────────────┐
   │ Survey      │  │ Survey Link      │  │ Survey     │
   │ Record Page │  │ Generator Tab    │  │ Dashboard  │
   └─────────────┘  └──────────────────┘  └────────────┘
            │                 │                 
            │                 │                 
            ▼                 ▼                 
   ┌─────────────────────────────────────────────┐
   │      surveyInvitations Component            │
   │      (Works in all contexts)                │
   └─────────────────────────────────────────────┘
                        │
                        │
            ┌───────────┼───────────┐
            │           │           │
            ▼           ▼           ▼
   ┌──────────────┐ ┌──────────┐ ┌──────────────┐
   │ Survey       │ │ Survey   │ │ Token        │
   │ Invitation   │ │ Object   │ │ Generator    │
   │ Controller   │ │          │ │ Service      │
   └──────────────┘ └──────────┘ └──────────────┘
```
